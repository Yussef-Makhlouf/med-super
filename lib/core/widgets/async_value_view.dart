// Pattern-matches AsyncValue<T>/Failure to the correct UI: retry banner for
// NetworkFailure, inline field errors for ValidationFailure, a distinct
// dialog for ConflictFailure — never a generic error toast (§6).
