import 'package:go_router/go_router.dart';

/// Intentionally empty: Phase 4 (Appointments) does not exist on the
/// backend yet (Phase 3 — Availability — is the current backend target).
/// Do not add real appointment hold/confirm/cancel routes here until that
/// backend phase ships — see
/// med-super/docs/backend_frontend_parity_matrix.md.
final appointmentRoutes = <RouteBase>[];
